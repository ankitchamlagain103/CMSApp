namespace Domain.Constants
{
    // The one well-known EmployeeCategory code the teacher-profile eligibility rule checks
    // against (PromoteToTeacherAsync / TeacherService.CreateTeacherAsync).
    public static class EmployeeCategoryCodes
    {
        public const string Academic = "ACADEMIC";
    }
}
